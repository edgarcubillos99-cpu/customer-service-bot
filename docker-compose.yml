services:
  app:
    build:
      context: .
      dockerfile: dockerfile
    container_name: whatsapp-teams-bot
    restart: always
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env # Carga todas las variables
    extra_hosts:
      - "host.docker.internal:host-gateway"